
#
# Spring and Spring Boot Settings
#
server:
    port: 9066

flyway:
    check-location: true
    initOnMigrate: true

logging:
    level:
        com:
            googlecode:
                flyway: "DEBUG"

#security:
#    basic:
#        enabled: false

#
# Archivist Settings
#
archivist:
    datasource:
        primary:
            url: "jdbc:h2:./db/archivist;DB_CLOSE_DELAY=-1;MODE=Oracle;"
            driver-class-name: "org.h2.Driver"
            username: "sa"
            password: ""
    events:
        port: 9087

    index:
        alias: "archivist"
    proxies:
        basePath: "proxies"
        format: "png"
    ingest:
        ingestWorkers: 1
    export:
        # Disable auto-export for tests, so we can run them manually
        autoStart: false
        basePath: "exports"
    snapshot:
        basePath: "snapshots"
        repoName: "backups"

