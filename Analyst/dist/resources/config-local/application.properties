# Override application defaults.

analyst.executor.threads = 1

logging.path=${analyst.path.home}/logs

# Bind the server to localhost so network interface ups
# and downs don't affect a local server.
server.address=127.0.0.1
