# Override application defaults.

logging.path=${analyst.path.home}/logs

# Blank out the server address so the server binds
# to an external IP address.  This is normally set to
# 127.0.0.1 for internal development.
server.address=
