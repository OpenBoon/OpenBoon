version: "3.6"
services:
  workbench:
    image: boonai/workbench
    ports:
      - 8888:8888
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      BOONAI_API_URL: http://api-gateway:80
    volumes:
      - "./data:/data"
