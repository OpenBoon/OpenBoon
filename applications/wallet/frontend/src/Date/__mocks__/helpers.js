const {
  formatFullDate,
  formatPrettyDate,
  getDuration,
  formatDuration,
} = jest.requireActual('../helpers')

const formatISODate = () => '2020-06-08'

export {
  formatFullDate,
  formatPrettyDate,
  getDuration,
  formatDuration,
  formatISODate,
}
