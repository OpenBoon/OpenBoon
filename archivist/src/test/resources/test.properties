
archivist.path.index=unittest/index
archivist.path.home=unittest
#
# Override the location of the unittest DB so it doesn't pick up data
# we add from running the server locally.
#

### The SQL DB vendor.
#archivist.datasource.primary.vendor=postgresql

### The JDBC URL to the Archivist SQL database.
#archivist.datasource.primary.url=jdbc:postgresql://localhost/chambers?currentSchema=zorroa

### The driver for the Archivist SQL database.
#archivist.datasource.primary.driver-class-name=org.postgresql.Driver

### The username for the Archivist SQL database.
#archivist.datasource.primary.username=chambers

### The password for the Archivist SQL database.
#archivist.datasource.primary.password=

#flyway.schemas=zorroa

archivist.datasource.primary.url=jdbc:h2:./unittest/archivist;DB_CLOSE_DELAY=-1;

#
# Run the the web server and event server on different port.
#
server.port=7066
archivist.events.port=7087
archivist.cluster.command.port = 7065
#
# Set the shared data path to the plugin installation spot.
#
archivist.path.shared=../unittest/shared

archivist.lfs.paths=${archivist.path.home}/../../../zorroa-test-data


spring.mail.host = smtp.gmail.com
spring.mail.port = 587
spring.mail.username=smtp@zorroa.com
spring.mail.password=yaqoxjsmuyqvdxof
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
