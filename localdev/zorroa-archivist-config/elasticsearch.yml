# Override elasticsearch defaults.

# To enable HTTP for tools like Kibana or Grafana, you must enable HTTP
# with cors support. Warning, the elastic port that is opened is unsecured.

#http.cors.enabled: true
#http.cors.allow-origin: "*"
#http.enabled: true

boostrap.mlockall: true
