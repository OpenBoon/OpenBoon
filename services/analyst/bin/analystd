#!/usr/bin/env python3
import signal
import sys

from analyst.main import main


def sigterm_handler(_signo, _stack_frame):
    sys.exit(0)


if __name__ == "__main__":
    signal.signal(signal.SIGTERM, sigterm_handler)
    main()
