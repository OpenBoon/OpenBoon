function passwordValidator(password) {
  // For now, check if password is longer than 8 characters
  // Change when password requirements are confirmed
  return password.length > 8
}

export default passwordValidator